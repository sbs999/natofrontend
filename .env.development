# API used while running `npm start`. Direct URL avoids CRA proxy missing routes.
# Change port if your backend uses a different PORT in natobackend/.env
# To use Render instead, set in .env.development.local:
# REACT_APP_BACKEND_URL=https://natobackend.onrender.com
REACT_APP_BACKEND_URL=http://localhost:8080
